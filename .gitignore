(docs/changelog.md, docs/system_map.md, docs/test_cases.md - these are documentation/source files, not build artifacts or dependencies)

(empty - no build artifacts, dependencies, or temp files to ignore)